[project]
name = "grouped"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "requests[socks]>=2.28 ; python_version < \"3.13\"",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
docs = [
    "sphinx==7.2.6",
]

[tool.poetry.dependencies]
python = "^3.11"
attrs = "^23.1.0"
